Func_with_many_args(a; b; c; d; e){
return a + b + c + d + e;
}

Func_with_While(condition){
    while(condition){
        a = 5;
    };
    return a + 4*8^2;
}

Fain(){
    skip;
}

Main () {
   v = 4;
   Fain();
   Func_with_many_args(1; 1; 1; 1; 1);
   Func_with_many_args(0; 10; 1; 2; 2);
   if (v == 3) {
        return 0;
   };
}
