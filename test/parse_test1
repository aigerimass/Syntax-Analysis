Func_with_many_args(a; b; c; d; e){
return a + b + c + d + e;
}

Func_with_While(condition){
    while(condition){
        a = 5;
    };
    return a + 4*8^2;
}

Fain(){
    skip;
}

Main () {
   v = 4;
   Fain();
   v = Func_with_many_args(1; 1; 1; 1; 1);
   v = v + Func_with_While(1);
   if (v == 3) {
        return 0;
   };
}
