Func_with_many_args(a; b; c; d; e){
    return a + b + c + d + e;
}

Func_with_While(bound){
    a = 10;
    while(bound > a){
        t = 1000;
        a = (a + 5);
    };
    return a + 4*8^2;
}

Fain(){
}

Main () {
   Fain();
   v = 10;
   v = v + Func_with_While(20;);
   g = (((1 > 0)));
   a = 10 / 2;
   if (g) {
       v = v ^ 2;
   };
   if (v == 4) {
        v = 1 * (-10) * Read();
   } else {
        v = v * 10;
   };
    while(v > 0){
        v = v / 10;
        Write(v;);
    };
}
